# Production Environment Variables for t4g.micro optimization

# Node build options (reduce memory usage during build)
NODE_OPTIONS=--max-old-space-size=512

# Docker optimization
COMPOSE_PARALLEL_LIMIT=1
DOCKER_BUILDKIT=1

# Nginx worker processes (auto-detect CPU cores)
NGINX_WORKER_PROCESSES=auto
